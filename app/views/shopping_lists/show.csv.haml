= CSV.generate_line([@shopping_list.shopping_day, @shopping_list.name])
- headers = ['Product', 'Quantity', 'Unit']
= CSV.generate_line headers
- @shopping_list_products.each do |shlp|
  = CSV.generate_line([product(shlp.product_id).name, shlp.quantity, product(shlp.product_id).unit])
