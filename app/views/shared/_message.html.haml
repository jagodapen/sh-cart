.mx-5.my-2
  - if notice
    %p.notice.alert.alert-success= flash.now[:notice]
  - if alert
    %p.alert.alert.alert-danger= flash.now[:alert]
    -# %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
    -# %ul
    -#   - @product.errors.full_messages.each do |message|
    -#     %p.alert.alert.alert-danger= message

  -# - if @product.errors.any?
  -#   #error_explanation
  -#     %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
  -#     %ul
  -#       - @product.errors.full_messages.each do |message|
  -#         %p.alert.alert.alert-danger= message
